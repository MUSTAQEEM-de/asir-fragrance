services:
  mongodb:
    image: mongo:7
    container_name: asir-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - asir_mongo_data:/data/db

volumes:
  asir_mongo_data:
